## 2026-05-08 - Focus-within for File Controls
**Learning:** Using `display: none` on hover states prevents keyboard users from accessing interactive controls like buttons in lists.
**Action:** Use `opacity: 0` and `pointer-events: none` combined with `:focus-within` to ensure keyboard accessibility.
